{
    "cmd": "supervisord -n",
    "container": {
        "docker": {
            "type": "DOCKER",
            "image": "webskel:latest",
            "network": "BRIDGE",
            "parameters": [],
            "portMappings": [
                { "containerPort": 5000, "hostPort": 0, "protocol": "tcp", "servicePort": 80 }
            ]
        }
    },
    "id": "webskel",
    "cpus": 0.25,
    "instances": 1,
    "mem": 32,
    "ports": [ 0 ]
}
